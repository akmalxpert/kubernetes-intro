apiVersion: v1
kind: Service
metadata:
  name: songs-db
  namespace: k8s-program
  labels:
    app: songs-db
spec:
  type: ClusterIP
  selector:
    app: songs-db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP

